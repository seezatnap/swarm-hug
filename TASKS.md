# Tasks

- [x] (#1) Add `--target-branch` CLI flag with auto-detected default (main/master); wire into config and add tests/docs as needed
- [x] (#2) Implement target branch auto-detection logic (check for main, then master, then current branch)
